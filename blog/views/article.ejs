<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>BLOG</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="/send_url.js"></script>
  </head>
  <body>
    
    <%- include('header'); %>

    <main>
      <div class="article">
        <!--一般記事の表示-->
        <% if(article.category === 'all') { %>
            <h1><%= article.title %></h1>
            <p><%= article.content %></p>
        <% } %>
        <!--限定記事の表示-->
        <% if(article.category === 'limited') { %>
            <i>会員限定</i> <!--バッジを表示-->
            <h1><%= article.title %></h1>
            <% if (locals.isLoggedIn) { %> <!--locals.isLoggedINの値がtrueの時限定記事の本文を表示する-->
                <p><%= article.content %></p>
            <% } else { %>
                <div class="article-login"> <!--locals.isLoggedINの値がfalseの時はログインを促す内容を表示-->
                    <p>今すぐログインしよう！</p>
                    <p>記事の続きは<br>ログインすると読むことができます</p>
                    <img src="/images/login.jpg">
                    <a class="btn" href="/login">ログイン</a>
                </div>
            <% } %>
            
        <% } %>
      </div>
    </main>
    <footer>
      <a class="btn sub" href="/list">一覧にもどる</a>
    </footer>
  </body>
</html>

