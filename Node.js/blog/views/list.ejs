<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>BLOG</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="/send_url.js"></script>
  </head>
  <body>
    <!--header.ejsを呼び出している-->
    <%- include('header'); %> <!--※<%=ではないため注意-->

    <main>
      <ul class="list">
        <% articles.forEach((article) => { %>
          <li>
            <!--限定記事の表示-->
            <% if(article.category === 'limited') { %>
                <i>会員限定</i>
            <% } %>
            <h2><%= article.title %></h2>
            <p><%= article.summary %></p>
            <a href="/article/<%= article.id %>">続きを読む</a>
          </li>
        <% }) %>
      </ul>
    </main>
  </body>
</html>
