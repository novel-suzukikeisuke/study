<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>LIST APP</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="/send_url.js"></script>
  </head>
  <body>
    <header>
      <a href="/" class="header-logo">LIST APP</a>
    </header>
    <div class="container">
      <div class="container-header">
        <h1>買い物リスト</h1>
        <a href="/new" class="new-button">+ 新規登録</a>
      </div>
      <div class="index-table-wrapper">
        <div class="table-head">
          <span class="id-column">ID</span>
          <span>買うもの</span>
        </div>
        <ul class="table-body">
          <!--forEachは配列の中の要素を1つずつ取り出して同じ処理をする-->
          <% items.forEach((item) => { %> <!--renderメソッドに記述したことでオブジェクトのプロパティで値を使える-->
            <li>
              <div class="item-data">
                <span class="id-column"><%= item.id %></span> <!--<%= %>はブラウザに表示できる-->
                <span class="name-column"><%= item.name %></span>
              </div>
              <div class="item-menu">
                <form action="/delete/<%= item.id %>" method="post"> <!--メモのidを受け渡すために送信先URLにメモのidを含める-->
                  <input type="submit" value="削除">
                </form>
                <a href="/edit/<%= item.id %>">編集</a>
              </div>
            </li>
          <% }) %> <!--<% %>は変数の定義等に使用-->
        </ul>
      </div>
    </div>
  </body>
</html>